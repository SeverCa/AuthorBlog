{% extends 'base.html.twig' %}
{% block body %}
    <h2>Editer un livre</h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Enregistrer</button>
    {{ form_end(form) }}

{% endblock %}