{% extends 'base.html.twig' %}
{% block body %}

<h2>Ajouter un livre, étape 1 : choisir la catégorie</h2>


<div class="form-type admin">
<p>Catégories existantes</p>
    {{ form_start(form2) }}
    {{ form_widget(form2) }}
    <button type="submit" class="form-button">Sélectionner</button>
    {{ form_end(form2) }}
</div>  


<div class="form-type admin">
<p>Créer une nouvelle catégorie</p>
    {{ form_start(form1) }}
    {{ form_widget(form1) }}
    <button type="submit" class="form-button">Enregistrer</button>
    {{ form_end(form1) }}
</div> 
{% endblock %}
